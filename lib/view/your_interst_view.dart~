import 'package:flutter/material.dart';
import 'package:task/view/dream_profession_view.dart';

import '../components/app_color.dart';
import '../components/custom_button.dart';

class InterstView extends StatefulWidget {
  const InterstView({super.key});

  @override
  State<InterstView> createState() => _InterstViewState();
}

class _InterstViewState extends State<InterstView> {
  double _selectedPercentage() {
    int selectedCount = dataList.where((item) => item['isSelected']).length;
    if (dataList.isEmpty) return 0.0;
    return selectedCount / dataList.length;
  }

  List<Map<String, dynamic>> dataList = [
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem', 'isSelected': false},
    {'title': 'sit amet', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem', 'isSelected': false},
    {'title': 'Lorem', 'isSelected': false},
    {'title': 'sit amet', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},

    {'title': 'sit amet', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
    {'title': 'Lorem', 'isSelected': false},
    {'title': 'Lorem', 'isSelected': false},
    {'title': 'Lorem ipsum', 'isSelected': false},
  ];

  @override
  Widget build(BuildContext context) {
    var height = MediaQuery.of(context).size.height;
    var width = MediaQuery.of(context).size.width;

    return Scaffold(
      body: SafeArea(
        child: Padding(
          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 16),
          child: SingleChildScrollView(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                SizedBox(height: height * 0.02),
                Center(
                  child: Stack(
                    children: [
                      // Background bar
                      Container(
                        height: 10,
                        width: width * 0.7,
                        decoration: BoxDecoration(
                          borderRadius: BorderRadius.circular(20),
                          color: Colors.grey[300],
                        ),
                      ),

                      // Foreground bar - based on selected count
                      Container(
                        height: 10,
                        width: (width * 2.0) * _selectedPercentage(),
                        decoration: BoxDecoration(
                          borderRadius: BorderRadius.circular(20),
                          color: AppColor.darkBlue,
                        ),
                      ),
                    ],
                  ),
                ),

                SizedBox(height: height * 0.05),
                Text(
                  'Your Interests',
                  style: TextStyle(
                    fontSize: 20,
                    fontWeight: FontWeight.bold,
                    color: AppColor.darkBlue,
                  ),
                ),
                SizedBox(height: height * 0.02),
                Text(
                  'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
                  style: TextStyle(fontSize: 13),
                ),
                SizedBox(height: height * 0.02),
                Container(
                  height: height * 0.06,
                  child: TextField(
                    decoration: InputDecoration(
                      filled: true,
                      fillColor: Colors.white,
                      prefixIcon: Icon(Icons.search, color: AppColor.lightBlue),
                      border: OutlineInputBorder(
                        borderRadius: BorderRadius.circular(20),
                        borderSide: BorderSide.none,
                      ),
                      labelText: 'Search',
                      hintStyle: TextStyle(color: AppColor.lightBlue),
                    ),
                  ),
                ),
                SizedBox(height: height * 0.02),
                GridView.count(
                  crossAxisCount: 3,
                  shrinkWrap: true,
                  physics: NeverScrollableScrollPhysics(),
                  mainAxisSpacing: 12,
                  crossAxisSpacing: 18,
                  childAspectRatio: 3.1,
                  children: List.generate(dataList.length, (index) {
                    final item = dataList[index];
                    return GestureDetector(
                      onTap: () {
                        setState(() {
                          dataList[index]['isSelected'] =
                              !dataList[index]['isSelected'];
                        });
                      },
                      child: Container(
                        padding: EdgeInsets.symmetric(
                          horizontal: 4,
                          vertical: 1,
                        ),
                        decoration: BoxDecoration(
                          color:
                              item['isSelected']
                                  ? AppColor.darkBlue
                                  : Colors.white,
                          border: Border.all(width: .3),
                          borderRadius: BorderRadius.circular(20),
                        ),
                        child: Center(
                          child: Text(
                            item['title'],
                            style: TextStyle(
                              color:
                                  item['isSelected']
                                      ? Colors.white
                                      : AppColor.darkBlue,
                            ),
                          ),
                        ),
                      ),
                    );
                  }),
                ),
                SizedBox(height: height * 0.06),

                CustomButton(text: 'Continue', onPressed: () {
                  Navigator.push(context, MaterialPageRoute(builder: (context) {
                    return DreamProfessionView();
                  },));
                }, isDark: true),
                SizedBox(height: height * 0.02),
                CustomButton(text: 'Back', onPressed: () {

                }),
              ],
            ),
          ),
        ),
      ),
    );
  }
}
